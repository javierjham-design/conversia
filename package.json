{
  "name": "conversia",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @conversia/database db:generate",
    "db:migrate": "pnpm --filter @conversia/database db:migrate",
    "db:setup": "pnpm --filter @conversia/database db:setup",
    "db:seed": "pnpm --filter @conversia/database db:seed"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
